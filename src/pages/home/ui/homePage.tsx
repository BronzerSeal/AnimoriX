import MainPageHeader from "@/widgets/main-header";

const HomePage = () => {
  return (
    <div className="flex min-h-screen items-center justify-center bg-zinc-50 font-sans dark:bg-black">
      <MainPageHeader />
    </div>
  );
};

export default HomePage;
